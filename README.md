# 🛠️ CRM для сервисного центра по ремонту техники

Полнофункциональная CRM система для управления сервисным центром.
Включает учёт клиентов, заказов и закупок запчастей в сервисном центре по ремонту техники.

## Основные возможности

- Клиенты (физ./юр. лица), уникальный телефон, адрес, компания
- Заказы:
  - статусы (в работе, на согласовании, ожидает запчасть, выполнено и т.д.)
  - услуги, аванс, автоматический расчёт стоимости и долга клиента
- Покупки запчастей с привязкой к заказу и статусами получения/установки
- Каталог услуг и категорий
- REST API (JWT) + Swagger / ReDoc документация
- Telegram‑бот для работы с CRM (просмотр данных)

## Архитектура

1. **Веб-интерфейс (Django Templates):**
    - Полный CRUD: клиенты, услуги, заказы, покупки
    - Дашборд с аналитикой
    - Административная панель Django

2. **REST API (Django REST Framework)**
    - Read-only для внешних интеграций
    - JWT аутентификация
    - Автодокументация (Swagger/Redoc)

3. **Telegram бот**
    - Поиск и просмотр клиента
    - Поиск / просмотр списка заказов по статусу
    - Просмотр списка покупок по статусу


## Технологии

- Backend: Python 3.11, Django 5.2, Django REST Framework
- База данных: PostgreSQL (prod), SQLite (dev)
- API: JWT аутентификация, OpenAPI 3.0, drf-spectacular
- Frontend: Django Templates, Bootstrap 5, JavaScript
- Инфраструктура: Docker, GitHub Actions CI/CD, Ruff, Black, pytest
- Документация: Swagger UI, ReDoc, автосхемы

## Структура проекта
```text
crm-project/
└── backend/
    ├── api/               # DRF-сериализаторы, viewsets, urls, документация API
    ├── crm/               # основное Django-приложение (модели, вьюхи, html-шаблоны)
    ├── telegram_bot/      # код Telegram-бота
    ├── tech_support/      # Django-проект: settings.py, urls.py, wsgi.py, asgi.py
    ├── manage.py
    └── requirements.txt
```

## Документация

1. Руководство для пользователя 

    - GET `/about/`
    - Подробнее: см. [backend/crm/README.md](backend/crm/README.md)

2. Документация API

    - JSON-схема: `GET /api/schema/`
    - Swagger UI: `GET /api/schema/swagger-ui/`
    - ReDoc: `GET /api/schema/redoc/`
    
    Подробнее: см. [backend/api/README.md](backend/api/README.md)

1. Telegram бот

    Подробнее: см. [backend/telegram_bot/README.md](backend/telegram_bot/README.md)

## Установка и запуск веб-сервера (локально)

1. Клонировать репозиторий
    ```
    git clone <URL_репозитория>
    cd crm-project/backend
    ```

2. Создать виртуальное окружение и установить зависимости
    ```
    python -m venv venv
    venv\Scripts\activate  # Windows
    # source venv/bin/activate  # Linux/macOS

    pip install -r requirements.txt
    ```

3. Настроить переменные окружения

    Создайте .env или экспортируйте переменные (пример):
    ```
    DJANGO_SECRET_KEY=секретный_ключ
    DJANGO_DEBUG=True
    DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

    # PostgreSQL
    DB_NAME=...
    DB_USER=...
    DB_PASSWORD=...
    DB_HOST=...
    DB_PORT=...

    # JWT/Telegram
    API_BASE_URL=...
    API_USERNAME=...
    API_PASSWORD=...
    TELEGRAM_BOT_TOKEN=...
    TELEGRAM_ADMIN_CHAT_ID=...
    ```

4. Применить миграции
    `python manage.py migrate`

5. Создать суперпользователя
    `python manage.py createsuperuser`

6. Запустить сервер разработки
    `python manage.py runserver`

Приложение будет доступно по адресу:
`http://127.0.0.1:8000/`

## Запуск бота

`python -m telegram_bot.main`


## Запуск в контейнерах Docker

```
# Разработка
docker-compose up

# Продакшен
docker-compose -f docker-compose.prod.yml up -d
```
