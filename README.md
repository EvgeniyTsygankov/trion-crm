# 🛠️ TRION - CRM для сервисного центра по ремонту техники

CRM система для управления сервисным центром: клиенты, заказы, услуги, закупки запчастей/программного обеспечения + read-only API для интеграций и Telegram‑бот для просмотра данных.

## Основные возможности

### Дашборд (главная страница)
- количество заказов и клиентов;
- количество активных заказов;
- суммарный баланс по всем заказам;
- список последних заказов.

### Клиенты
- физ./юр. лица (`FL/UL`);
- уникальный телефон в формате `+7XXXXXXXXXX`;
- адрес / компания;
- правило: для юр. лиц `company` обязательно (валидация и constraint).

### Заказы
- статусы (в работе, на согласовании, ожидает запчасть, выполнено и т.д.);
- услуги (M2M через `ServiceInOrder` с фиксацией цены на момент добавления);
- аванс, оплата;
- автоматический расчёт:
  - `services_total` (с учётом `services_total_override`),
  - `purchases_total`,
  - `total_amount`,
  - `duty = total_amount - advance - paid`;
- генерация кода заказа вида `TN-00001` (на основе поля `number`).

### Покупки (запчасти/ПО)
- покупка может быть привязана к заказу или быть без заказа;
- статусы: ожидается поставка / получено / установлено;
- стоимость участвует в расчёте итоговой суммы заказа.

### Каталог услуг
- категории услуг со `slug` добавляются через Django Admin;
- услуги: уникальное название + базовая стоимость;
- удаление услуги запрещено, если она используется в заказах.

---

## Архитектура

1) **Веб-интерфейс (Django Templates)**
- полный CRUD: клиенты, услуги, заказы, покупки;
- дашборд;
- Django Admin.

2) **REST API (Django REST Framework)**
- read-only API для интеграций и Telegram‑бота;
- JWT‑аутентификация (Djoser + SimpleJWT);
- OpenAPI документация (drf-spectacular).

3) **Telegram‑бот**
- авторизация по JWT через логин/пароль пользователя CRM;
- поиск клиента по телефону;
- поиск заказов и фильтр по статусам;
- просмотр покупок по статусам.

---

## Технологии

- Python 3.11
- Django 5.2, Django REST Framework
- PostgreSQL (Docker/prod), SQLite (локальная разработка при `DEBUG=True`)
- Auth: JWT (SimpleJWT), Djoser
- API docs: drf-spectacular (Swagger UI / ReDoc)
- Telegram: pyTelegramBotAPI
- Infra: Docker, Docker Compose, nginx (gateway)
- Tests: pytest, pytest-django
- Code quality: Ruff, Black, pre-commit

## Структура проекта
```text
trion-crm/
├── .github/workflows/         # CI/CD GitHub Actions
├── backend/
│   ├── api/                   # read-only DRF API (viewsets, urls)
│   ├── crm/                   # основное Django-приложение (модели, views, шаблоны)
│   ├── telegram_bot/          # Telegram-бот
│   ├── tech_support/          # Django проект (settings/urls/wsgi/asgi)
│   ├── fixtures/              # demo.json
│   ├── manage.py
│   └── requirements.txt
├── gateway/                   # nginx/gateway
├── docker-compose.yml
├── docker-compose.production.yml
└── README.md
```
---

## Документация

1. Руководство для пользователя (web)
   - `GET /about/`
   - Подробнее: см. [документацию](backend/crm/README.md)
   - Кастомные страницы ошибок:
     - `403` — нет прав (и отдельная страница для CSRF)
     - `404` — страница не найдена
     - `500` — ошибка сервера
     - Шаблоны: `backend/templates/pages_errors/`

2. Документация API
    - JSON-схема: `GET /api/schema/`
    - Swagger UI: `GET /api/schema/swagger-ui/`
    - ReDoc: `GET /api/schema/redoc/`
    - Подробнее: см. [документацию API](backend/api/README.md)

3. Telegram бот
    Подробнее: см. [документацию Telegram-бота](backend/telegram_bot/README.md)

---

## Read-only API: кратко по ресурсам

### JWT endpoints

- `POST /api/auth/jwt/create/` → `{access, refresh}`
- `POST /api/auth/jwt/refresh/` → `{access}`

## Clients

- `GET /api/clients/?search=...` — поиск по телефону (параметр обязателен)
- `GET /api/clients/{id}/`

## Orders

- `GET /api/orders/`
- `GET /api/orders/{id}/`
- поддерживается:
  - `?status=...`
  - `?ordering=id` / `?ordering=-id`
  - `?search=...` (по оборудованию, телефону клиента; если есть цифры — по number)
- в ответе заказа:
  - есть client_name, mobile_phone
  - есть список услуг services
  - есть вложенный список покупок purchases

## Purchases

- `GET /api/purchases/`
- `GET /api/purchases/{id}/`
- поддерживается:
  - `?status=...`
  - `?ordering=id` / `?ordering=-id`
  - `?search=...` по `detail`
- в ответе покупки:
  - есть `order_code`
  - есть `client_name`
  - поле `order` (ID) не возвращается

---

## Telegram‑бот

Запускается отдельным сервисом bot в docker-compose.
Работает поверх API и требует:

- `TELEGRAM_BOT_TOKEN`
- `API_BASE_URL` (в docker-compose: http://backend:8000)
- опционально `TELEGRAM_ALLOWED_CHAT_IDS`

Команды/кнопки:

- `/start` — старт и авторизация
- `/help` — справка
- кнопки: Клиенты / Заказы / Покупки

---

## Демо-данные (fixtures)

В репозитории есть фикстура с демонстрационными данными (клиенты, заказы, покупки, категории и услуги).
Данные тестовые и не содержат персональных данных реальных клиентов.

Все данные являются демонстрационными и не содержат персональных данных реальных клиентов.

Файл фикстуры:
- `backend/fixtures/demo.json`

> В фикстуре нет пользователей и суперпользователя (`auth.User`). После загрузки создайте суперпользователя отдельно.

---

## Установка и запуск веб-сервера (локально, без Docker)

1. Клонировать репозиторий
    ```bash
    git clone git@github.com:EvgeniyTsygankov/trion-crm.git
    cd trion-crm/backend
    ```

2. Создать виртуальное окружение и установить зависимости
    ```bash
    # Windows
    python -m venv venv
    . venv\Scripts\activate
    pip install -r requirements.txt
    ```

    ```bash
    # Linux/macOS
    python3 -m venv venv
    source venv/bin/activate  
    pip install -r requirements.txt
    ```

3. Настроить переменные окружения

    Создай файл backend/.env (пример):
    ```
    SECRET_KEY=***
    DEBUG=True  # True для SQLite, False Для PostgreSQL
    DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

    # Если используете PostgreSQL локально:
    POSTGRES_DB=crm_db
    POSTGRES_USER=crm_user
    POSTGRES_PASSWORD=***
    POSTGRES_HOST=127.0.0.1
    POSTGRES_PORT=5432

    # Telegram-бот
    TELEGRAM_BOT_TOKEN=***
    TELEGRAM_ALLOWED_CHAT_IDS=1234567
    
    # URL API, куда ходит бот
    API_BASE_URL=http://127.0.0.1:8000
    ```

4. Применить миграции
    `python manage.py migrate`

5. Создать суперпользователя
    `python manage.py createsuperuser`

6. Запустить сервер разработки
    `python manage.py runserver`

Приложение будет доступно по адресу:

- CRM web: http://127.0.0.1:8000/
- Admin: http://127.0.0.1:8000/admin/

---

## Запуск Telegram‑бота (локально)

Из каталога 'backend/':

```bash
# активируй venv
python -m telegram_bot.main
```

Условия:
- backend запущен
- API_BASE_URL указывает на работающий backend
- задан TELEGRAM_BOT_TOKEN

---

## Запуск проекта в Docker Compose (backend + nginx + bot + Postgres)

1. Заполнить переменные окружения (DEBUG=False)

2. Запустить сервисы:

```bash
docker compose up -d --build
```

3. Далее выберите один из сценариев

### Сценарий A. Запуск с созданием новой БД

При первом запуске:

```bash
docker compose exec backend python manage.py migrate
docker compose exec backend python manage.py collectstatic --noinput
docker compose exec -it backend python manage.py createsuperuser
```

После этого:
- CRM web (через gateway/nginx): http://127.0.0.1/;
- Admin: http://127.0.0.1/admin/
- Swagger UI — http://127.0.0.1/api/schema/swagger-ui/
- Telegram‑бот запускается как `bot` из docker-compose.yml.

Для остановки:

```bash
docker compose down
```

Полное удаление volumes (включая БД):

```bash
docker compose down -v
```

### Сценарий B. Запуск с демо‑данными

Внимание: `flush` удалит текущие данные из БД.

```bash
docker compose exec backend python manage.py migrate
docker compose exec backend python manage.py flush --no-input
docker compose exec backend python manage.py loaddata fixtures/demo.json
docker compose exec -it backend python manage.py createsuperuser
```